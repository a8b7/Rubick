server:
  host: "0.0.0.0"
  port: 8080
  mode: "debug"  # debug, release, test

database:
  type: "sqlite"
  path: "./data/rubick.db"

docker:
  default_host: "local"

logging:
  level: "info"
  format: "json"
